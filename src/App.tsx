import React from 'react';

interface AppProps {}

function App({}: AppProps) {
  return (
    <div>
      <h1>Product feedback!!!</h1>
      <button>Click</button>
    </div>
  );
}

export default App;
